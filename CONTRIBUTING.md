# Contributing

## Development

```bash
npm install
npm run dev
```

## Pull requests

- Run `npm run lint` and `npm run build`
- Keep UI strings generic and avoid embedding private identifiers
- Do not add real social-platform automation unless it uses official APIs and is clearly documented

## Style

- Prefer small, focused changes
- Keep “local-first demo” assumptions explicit in docs

